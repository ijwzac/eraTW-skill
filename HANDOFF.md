# HANDOFF — eraTW 口上(kojo)写作 SKILL + 露娜(K6)方法论实测用例

> ---
> **⚠ 接手 / compact 后：在动任何代码之前，先【完整读】以下内容——**
> 1. 本 skill 的 `SKILL.md` **全文**；
> 2. `references/` 里**全部 A 类必读文件**（见 `SKILL.md` §0.4 的三层清单，逐个读完，别只扫）；
> 3. 与接下来要做的任务**相关的 B 类 reference 全文**（要写/改命令 → `12-命令速查`；碰日记 → `14`；碰依赖 → `15`；碰刻印 → `16`；搭骨架/找标签 → `01`……沾边就整份读完）。
>
> **compact 摘要不能替代这一步**——它会丢掉大量关键细节。读完上面三项，再往下看本文件。
> ---

> **另一份权威现状**：记忆文件 `C:\Users\lzh\.claude\projects\d--Game-TouHou-Yoiyami-Dreamer-datas-eraTW\memory\luna-kojo-pending-todo.md`，按批次记录了全部引擎调查结论与改动史。本文件写"接手者必须知道但代码和 git log 答不了"的东西；记忆文件写"查过的引擎事实"。两份互补，都要读。

---

## 一、用户是谁 / 铁律（连同为什么）

用户是中国人，用中文交流，是这套口上的作者兼玩家，**不是**程序员出身但对工程严谨度要求很高。他会用平实语言描述剧情需求，把它翻译成引擎标签和控制流是 AI 的活。

**铁律（违反的代价用户往往几十小时后才发现、且不可逆）：**

1. **写操作只许落在**：露娜口上目录 + `learning/` + `emuera.config`（已备份 `emuera.config.bak_clip`）。
   **为什么**：用户的游戏目录里属于"我们的东西"只有那一个角色目录，其余全是别人的资产——引擎代码、上千个第三方作者的口上、用户玩了几百小时的存档。改坏任何一样，用户可能很久以后才撞见，那时无从追溯。**即使确信引擎有 bug、即使从那儿改明显更省事，也不改。**
   发现问题在口上之外时的正确做法＝**报告根因（文件:行号）+ 给用户可自行执行的调试命令，让用户决定**。已有先例：露娜掏耳朵没反应，真因是引擎 `COMF305` 的体型闸，正确处置是告知 + 给 `TALENT:0:体型 = 0` 自查法，而不是去改 `COMF305`。
2. **ERB 文件必须 UTF-8 带 BOM**（`open(p,'w',encoding='utf-8-sig')`）。**为什么**：没 BOM 时中文槽位名（`TALENT:恋人`）在某些字符串上下文静默匹配失败——不报错，只是行为错。写入工具默认剥 BOM，**每次 Edit/Write 后都要重查**。
3. **`.ps1` 也必须带 BOM**。**为什么**：PowerShell 5.1 读无 BOM 的含中文 ps1 会按 GBK 解码 → 语法错误。`.md` 不需要 BOM。
4. **CJK 不要走 PowerShell 命令行参数传递**（会乱码）——用 Bash 或 Python，或从文件读。
5. **含 `[ルナ]` 方括号的路径**：Python 用 `os.listdir` 不要用 `glob`（方括号＝字符类）；PowerShell 用 `-LiteralPath`（方括号＝通配符）。这个坑重复踩过多次。
6. **改 ERB 后必跑校验**：`count(^IF)==count(^ENDIF)`、`SELECTCASE==ENDSELECT`、BOM 在，然后同步到 `eratw-skill/reference-kojo/luna-K6/`。改 ps1 后跑 PARSE 校验。
7. **往公开仓库推之前，先扫待提交文件里有没有真实姓名等 PII**。**为什么**：仓库是 PUBLIC，曾经差点把含玩家真名约 2300 次的 `cliplog*.txt`/`test_result.txt` 推上去。它们已进 `.gitignore`（本就是每次重生成的测试产物），历史干净。存档里的玩家名也已匿名化处理过。
8. **新增 SKILL 内容一律用中文**（标签名/代码标识符/必要术语保留原文）。术语约定：test battery ＝「**测试套件**」，不要用「电池」。
9. **不确定就停下来问**。没有"但这次情况特殊"的例外。
10. **备份方案＝GitHub（SKILL §0.9 的 C 案）**，用户已明确选定：**每次改完露娜口上 → 同步到 `learning/eratw-skill/reference-kojo/luna-K6/` → commit / push 到 `github.com/ijwzac/eraTW-skill`**。
    **为什么这条要写死**：露娜的真源在 `ERB/…/露娜切露德_重制/`，那个目录**不在 git 仓库里**——**只有镜像进了仓库**。所以「同步镜像」不是可有可无的整理工作，**它就是备份动作本身；漏同步 = 那一版没有备份，而且是静默的**。
    ⚠**已经发生过**：一次检查发现 6 个 ERB + `autotest.txt` 漂移（测试回写只落在游戏目录、镜像停留在几天前）。**所以每次 commit 前先跑一次「游戏目录 vs 镜像」逐文件哈希比对**，别靠记忆。
    另注：`口上备份/露娜切露德_重制_20260718-0139/`（游戏根下、`ERB/` 之外）是一份一次性时间戳快照，保留即可，**不要删**（§0.9：备份只增不减）。
    ⚠**GitHub 仓库是 PUBLIC**：每次推送前照例扫一遍待提交文件里有没有真名等 PII（`cliplog*`/`test_result*` 已 gitignore）。

---

## 二、项目背景与目标

**做什么**：打磨 **eratw-skill**——一个帮助 LLM（Claude Code 等）为 eraTW 写角色口上的 Claude Skill。eraTW 是基于 Emuera 引擎的东方 Project 文字 RPG，约 150 位角色，每位角色的对话+行为是一套 `.ERB` 脚本，引擎按约定的标签名把事件"派发"进去。

**怎么打磨**：**从零写露娜切露德（K6）的口上，作为方法论的实测用例**——不是为了写露娜而写露娜，而是"用 skill 写一个真角色，撞到什么坑就把知识回填进 skill"。skill 里绝大多数引擎知识都是这么来的。

**仓库**：`github.com/ijwzac/eraTW-skill`（用户 = ijwzac，**PUBLIC**），工作副本在 `learning/`。

**对照实验（进行中）**：用户让**另一个 AI** 照露娜的丰富度、用这份 skill 从零写**斯塔萨菲雅（K7）**口上，检验 skill 是否足够自足。需求文档在游戏根 `斯塔口上丰富度需求_给斯塔AI.md`（git 仓库外）。
**⛔ 斯塔的文件只诊断、绝不修改**（`007 Star [スター]/スターサファイア/`）。已发现但未改的问题：`;@AT` 标签错放进 AUTOTEST.ERB（应只放正文）、HANDOFF 过短、运行时崩溃 `RAND:LOCAL:1`（应为 `RAND:(上限)`）。

**发布策略（已定案）**：skill 附带**两份示例口上**——`reimu/`（灵梦 K1，较老的单体填充范例）+ `luna-K6/`（现代多文件范例）。
**露娜的测试脚手架【全部保留】**（AUTOTEST.ERB + 会話钩子 + `[[MT]]` 标记 + `;@AT` 标签）——因为它是 references/11 方法论的**唯一真实范例**，抽象文档替代不了。`luna-K6/README.md` 里有醒目警告块区分「教学范例（故意保留）」vs「发布给玩家的口上（应清理）」两种语境，别混淆。
**SKILL/references 正文里除非必要不要用 K6 举例**——通用指导保持角色无关（写 `K{id}`）。但**复杂到有必要看真实代码的地方，可以明确写"见 `reference-kojo/luna-K6/` 的某文件某处"**，把露娜当活教材指过去，而不是把露娜的细节抄进正文。

---

## 三、当前状态

**skill 结构**（`learning/eratw-skill/`）：
- `SKILL.md` 主体——**§0.4 ⭐references 三层读取策略**（规则非建议）/ **§0.7 ⛔作业边界** / **§0.8 HANDOFF 自觉维护** / §1 十三条常见初稿坑 / §2 校验流程 + **§2.1 测试覆盖硬门槛** / §5-§9 分发机制与标准形态 / **§9.5 内容自查四点** / §12 社区教程语料库。
- **references 三层（§0.4）**：**A 层动手前必读**（~30k）＝ 02状态总线 / 03helper / 04DSL / 05事件ARG / 07其它 / 09人设 / 10编码 / **11自动测试** / 13惯用法；**B 层沾边就读全文**（别 grep 一行就走）＝ 12命令速查 / 01标签目录 / 06recipes / 14日记 / 15依赖 / 16刻印；**C 层只 grep 永不整份读**＝ 08角色名表 / `data/**`（~190 CSV，是核查基准非读物）/ `data/engine/*.ERB` / **`reference-kojo/reimu`（~236k token，其中 `コマンド.ERB` 一个就 183k，能撑爆 200k 窗口）**。
- 全部 references 与 SKILL.md **已汉化**（`08` 罗马名表保持英文）。**临时的 `zh/` 平行译文目录已删除——正式位置即唯一事实源，别再建平行译文目录**（两份必然漂移，收拾过一次了）。

**露娜 K6 现状**：`ERB\口上・メッセージ関連\個人口上\006 Luna [ルナ]\露娜切露德_重制\`，15 个 `.ERB` + AUTOTEST 测试套件 + `autotest.txt`。日常/社交/事件对话已充分填写（偏 SFW），成人类目留 `LOCAL = 0` 结构桩。测试套件当前 117 对 TID（BEGIN=OK），静态校验全过。

---

## 四、测试流水线怎么跑（当前机制，⚠ 旧文档说法已作废）

**一键入口**：双击 `learning/eratw-skill/run_eratw_test.bat`（以 `-STA` 启动 `start_pipeline.ps1`）→ 选 dev/player 版 → 启游戏 + 后台 `clip_tap` 实时抓词到 `cliplog.txt` → 关游戏后崩溃诊断 + 扫标记 + 回写 `;@AT` 状态 + 生成 `test_result.txt` + 把本次会话小结追加到 cliplog 末尾。
**bat 里所有需要选择的地方都同时接受字母和数字**（Y 或 1 / N 或 2 / A 或 1 / P 或 2）。
**要排查或修改流水线，先读脚本本身**——`run_eratw_test.bat`(壳) / `start_pipeline.ps1`(主编排) / `tools/clip_tap.ps1` / `tools/at_update.ps1` / `tools/manual_scan.ps1` / `tools/disarm_autotest.ps1`。references/11 §11.12 的导览会落后于脚本。

### 自动测试开关 ＝ `autotest.txt` 内容开关（现行机制）
口上目录里有一个**永久存在**的 `autotest.txt`，**内容**是 `on` 或 `off`：
1. bat 靠"这个 `.txt` 存在 + 它的路径"发现哪些口上是本 skill 写的，并读内容显示已启用/已禁用；启用/禁用＝改写内容（`WriteAllText`，不改名）。
2. 会話钩子（`日常系コマンド.ERB` 的 `@M_KOJO_MESSAGE_COM_K6_300` 顶部）`LOADTEXT` 读它，`STRCOUNT(RESULTS,"on")` 为真则 `CALL` 测试套件。
3. 测试套件结尾 `SAVETEXT "off", "<同一路径>"` 自动关掉。
4. **自动测试因此与存档完全无关**——这正是想要的。已弃用 `CFLAG:6:1099`（旧的三态 arm 位）和 `autotest.on/off` 文件名编码法。

**⚠ 三条承重的引擎事实（都是踩出来的）：**
- **`LOADTEXT` 成功读取后 `RESULT` 也是 0**（不是 1、不是行数）。第一版钩子写成 `IF RESULT && STRCOUNT(...)` 被永久短路成"从不触发"，查了很久。**只用 `STRCOUNT`，且 `LOADTEXT` 前先 `RESULTS = ""` 清空**防文件不存在时残留上一个值。
- **`SAVETEXT`/`LOADTEXT` 的字符串第二参数＝路径，相对游戏根，必须用正斜杠 `/`，且只允许 `.txt`**（`emuera.config:72` 白名单）。用反斜杠 `\` 不报错，但整条路径会被拼成一个怪文件名堆在游戏根目录。
- **Emuera 的 `SAVETEXT` 写文件会加 UTF-8 BOM**；无害，`STRCOUNT` 和 .NET `ReadAllText` 都对开头 BOM 免疫。
- 补充：**Emuera 没有 `DELETEFILE`/`RENAMEFILE`**，ERB 不能删/改名文件——所以开关只能靠"改内容"而不是"改名/删文件"。

### 其余测试机制
- **`;@AT <状态> <TID>` 标签**：状态 ∈ `待自动测试 · 待手动测试 · 测试通过 · 测试失败`；TID 形如 `K6_410_旁观`，全局唯一。**标签只写正文，AUTOTEST.ERB 里只放 `[[TID]]`**（斯塔那个 AI 两边都写，是错的）。
- **判定＝END 哨兵门控**：日志里出现 `=====AUTOTEST_K{id}_END=====` 就说明套件跑完没崩 → 所有打了 BEGIN 的 TID 一律通过。这是为了**免疫剪贴板丢字造成的假失败**。没有 END ＝ 崩了，崩的是"日志最后一个 marker 恰为某 TID 的 BEGIN"那条。
- **多口上兼容**：`Get-AutotestBlocks`（复数）返回所有口上的块并逐个回写；`at_update.ps1 -Cid` 把解析隔离在某个角色的 BEGIN..END 之间。**这两处都是修过的真 bug**——原来只回写最后一个口上、且全局 END 会把另一个口上的崩溃误判成通过。
- **手动测试**：`待手动测试` 分支打两行——守卫行 `SIF K6_MT_ON()` 紧接 `PRINTL [[MT <TID>]]`。`manual_scan.ps1 -Apply` 回写并删标记时会**连带删掉紧邻其上的守卫行**（否则孤儿 SIF 会吞掉下一行真台词）。
- **`@K6_MT_ON()` 总开关**（函数库里的 `#FUNCTION`）：开发期 `RETURNF 1`，发布前翻 `RETURNF 0` → 一处翻转让全部 `[[MT]]` 对玩家静默，安全但难测的分支可带标记发布，不必强删。
- **`CFLAG:6:1098` ＝ 强制台词号**（>0 时 300 会話跳过所有随机、直接演该编号），测试套件逐条 set 它，让候选池里每条台词都能确定性触发、TID 与标签 1:1。
- **错误诊断（两次检查，都只读 `emuera.log`、不读 `cliplog`）**：
  **①编译/脚本错误**（在 autotest/mt **之前**）＝ `emuera.log` 新鲜且 **<50 行**且含 `警告Lv\d`/`Emuera停止运行`/`无法解释的行` → 并全文进 cliplog + 汇报 + **直接结束**（编译有错时标签会静默失效，测试结果不可信）。
  **④运行时错误**（在 autotest/mt **之后**）＝ `emuera.log` 新鲜且**末 20 行**含 `错误发生`/`错误内容`/`函数调用栈` → 并末 20 行进 cliplog + 汇报；**测试结果照常回写**（崩溃前跑到的部分仍有价值，所以这一步放最后）。
  **为什么都不用 cliplog**：`cliplog` 是 clip_tap 轮询**剪贴板**的实时流，而引擎的编译警告与异常堆栈是引擎**直接写屏 + 写 emuera.log**、**不走 PRINT→剪贴板**那条路，剪贴板里可能压根没有。⚠**旧版用 cliplog 判编译错误漏报过**：真实案例 cliplog 100 行、无「Emuera停止运行」，但引擎报了一串 `警告Lv2`。
  `emuera.log` 由引擎写在游戏根、**只在加载/运行时错误/手动保存时更新**，启动器每次启动会先删它，故存在即本次会话。

---

## 五、踩过的坑与教训（含试过但走不通的路）

**这些是最省未来时间的部分——不看会原样再踩一遍：**

- **⭐`lazyloading.dat` 是"新加标签在游戏里不触发"的元凶**。`USELAZYLOADING:YES`（玩家默认）下，**新增**的标签不在旧符号索引里 → 引擎当它不存在 → 回退到通用叙述（玩家看到命令横幅但没有你的台词）；而**已存在标签的正文改动**会正常生效——**这个不对称最坑**。查了两轮代码才发现代码本身是对的。修法：删 `lazyloading.dat` 再启动。启动器已会自动删，手动启 exe 要手删。
- **`#DIMS ARGS` 让整个游戏启动即 halt**。`ARGS` 是 Emuera 保留位置参数，自动可用，**绝不能 `#DIM`/`#DIMS` 声明**。同理 `ARG`/`RESULT`/`LOCAL`/`TARGET`。
- **`FLAGSETTING` 不是"读档时调一次"**——`SYSTEM.ERB:75 @EVENTLOAD`（读档）**和** `INFO.ERB:922 @INFO_RENEW_TARGET`（**每次刷新目标/状态信息**）都会逐角色调用。**所以 FLAGSETTING 里的东西必须幂等、绝不能有每次执行的副作用**。旧的哨兵 arm 方案就是栽在这——去掉 `!= 2` 守卫会导致"每点一次会話就重跑一整轮测试"。
- **`GIFT` 的 ARG2 与 ARG3 会连发**，不是二选一。分档：1 回赠 / 2 珍藏≥700（**每次都发**）/ 3 刷新最爱（≥700 且破纪录）/ 4 普通收下 400-699（**最常见**）/ 5 变卖<400。評価点 0-999，中性礼≈500 落 ARG4，命中一个强喜好≈666 仍是 ARG4，≥700 要命中 4 个以上喜好 tag，≥800 极罕见。**所以重要解锁要挂 ARG2，别挂 ARG3 或再叠 ≥800 判定**（原来就是这么写的，等于永远不触发）。
- **约会结束有三条互不调用的路径**（写错 handler 台词就永远不出）：① 玩家手动点「从外面回家」→ `@DATE_EVENT` → 未吻发 SP_EVENT1 初吻 / 达阈发 SP_EVENT2 告白 / **其余落 SP_EVENT3 兜底（玩家最常走）**，**从不调 `@KITAKU`、EVENT_20 永不触发**；② 角色自己到点回家 → `@KITAKU` → EVENT_20，但**住人**（露娜默认是住人）到就寝时间走的是 **EVENT_3 おやすみ**、根本不发 EVENT_20；③ 倦意归途（`NEMUKE()>=720`，与体力无关）默认不发口上。**露娜初版把 SP_EVENT3 留成空桩，导致"约会归来什么都没有"——这就是踩坑返工的真实案例。**
- **`CFLAG:N:约会中` 不是布尔值**，是 MAIN_MAP 编码。任何一次约会之后它永久非零，`IF CFLAG:N:约会中` 此后恒为真。正确谓词＝`CHK_DATENOW(CFLAG:MASTER:约会中) && FLAG:约会的对象 == TARGET`。
- **槽位名必须与 CSV 逐字节匹配**：本 fork 用简体（`约会中`），日文汉字 `約会中` 无法解析且不报错。`BASE:N:疲労` 根本不存在（用 `気力 < MAXBASE/2`）。
- **`[[X]]` 在 `Str.csv` 里查不到时静默编译成 `0`**——大多数角色名都不在里面，`CASE [[ルナサ]]` 会变成 `CASE 0` 意外匹配 ARG=0。**默认用数字 ID + 注释**。
- **EVENT_1 每次格子迁移都触发**（不是"进房间时一次"），必须第一道守卫 `SIF CFLAG:{id}:現在位置 != CFLAG:MASTER:現在位置 / RETURN 0`。但 **EVENT_4「移動時すれちがい」相反，禁写同格守卫**（ARG2/3 本就不同格）。
- **剪贴板丢字的真凶是 `emuera.config` 第 81 行** `画面のリフレッシュ時にクリップボードとバッファを消去する`——画面刷新即清空剪贴板，边界上的标记还没被抓到就没了。已改 `NO`，并把 `総バッファサイズ 300→1000`（原来比"貼り付ける行数 500"还小）。
- **走不通的路（别再试）**：Emuera **没有**命令行设变量、**没有**启动 autoexec；调试台**拒绝执行流程控制命令**（`CALL`/`IF`/`GOTO` 都被拒），所以没法从外部触发测试——这就是为什么测试套件必须寄生在玩家一定会点的「会話」命令上。改存档二进制来 arm 测试＝高危，否决。`emuera.config` **没有**"退出时 dump 完整历史"的设置，做不到保证每次都有完整 emuera.log——但不必，两个日志互补就够。
- **Python 改文件时，断言要在写盘前跑**。有一次锚点缩进猜错（8 空格 vs 实际 12），断言先失败、文件没被动过，改对再跑——这个顺序救了一次。

---

## 六、角色约定（compact 会丢的设定）

**露娜切露德（Luna Child，K6）**：三妖精之一，**夜之月光妖精**。人设＝迟钝、爱讲理（爱抬杠）、**三妖精里的头号胆小鬼**。能力＝**消音**（响指让周围无声）。癖好＝偷咖啡喝、动不动摔跤。
**⚠ 作息＝就寝 2:00 / 起床 8:00，不是昼夜颠倒**——她只是三妖精里睡最晚起最晚的。别再写"夜里根本睡不着/白天是深夜"（EVENT_2/3 已按此重写过一次）。
**同伴**：桑尼米尔克（K5，红之光）、斯塔萨菲雅（K7，星之光）。三妖精常一起行动、一起恶作剧，口上里频繁穿插三人组的味道。「捉弄人是三妖精的本职」——所以摸头之类她不会单纯害羞，而会反过来恶作剧回敬。
**私室房间码 33**；作为住人时在三妖精之家（MAIN_MAP 0 的房号 26-35）。**非同住角色去她房间会被传送到通用待客室（`98 + MAIN_MAP*100`），真·33 抵达不了**——所以写"在她房间里"的专属台词要注意这个传送陷阱。
**体型 = -2（幼児体型）**，这导致「索求膝枕(305)」对体型较大的主人必定"青蛙失败"，进而「掏耳朵(SCOM60)」连招必断。**这是引擎 `COMF305` 的体型闸，不是口上的问题**，口上侧无需改。
**内容尺度**：SFW 为主，成人类目（性交系/加虐系/絶頂/353）留 `LOCAL = 0` 结构桩。

**好感度刻度参考**：<250 生疏 / <500 点头之交 / <1000 熟人 / <1500 好朋友 / <3000 思慕（`TALENT:思慕` 门槛之一即好感度≥1500）/ ≥3000 恋慕或恋人。

---

## 七、待办

**A. 下次实机测试要验证的**（代码写好了但没跑过）
- 会話候选池的约会码 70-73 四档、GIFT 七档、`K6_COLOR` 不再报孤儿警告
- 闯入厕所/浴室的好感/信赖数值变化打印（这条只能手动测）

**B. 测试套件还没接线的分支**（仍是"待手动测试"，按 §11.13 ⭐原则应尽量改成自动测）
依頼系约 15 条、EVENT_20/26/27/4、RUN_INTO、SPEVENT_2/3、月見、UNIQUE_COUNTER1、MARKCNG、弾幕、410/411/416

**C. SKILL 正文的 K6 去特化**（发布策略已定，见第二节）
把 SKILL.md 与 references 里非必要的 K6 举例改成角色无关的 `K{id}`；确实需要看真实代码的地方，改写成"见 `reference-kojo/luna-K6/<文件>` 的某处"指过去。当前 K6/露娜出现最多的是 `references/11`（自动测试）和 `references/13`（高频惯用法）。

**D. 零散**
- `references/11` 的 §11.9/§11.11/§11.12 里还残留旧 `CFLAG:1099` arm 法的描述（§11.14 已是权威、顶部也加了指针，但旧文字没清）
- EVENT_20 可补 CASE 7/8/9（留宿），当前只到 CASE 6
- 309 摸头扩成多分支（像 302 那样按心情/关系/好感度分层）
- `関数ライブラリ.ERB` 会被 §2 的"有台词却 0 标签"启发式报出来——**但那是 helper 文件，按 §2.1 豁免（台词覆盖体现在调用方），非缺陷、无需改**

**E. 发布给玩家时的清理清单**（仅当发布"给玩家玩的露娜"时；作为 skill 教学范例的 `luna-K6/` **不做**这些）
删 AUTOTEST 钩子 + `AUTOTEST.ERB`、翻 `@K6_MT_ON()`→`RETURNF 0`、删 `autotest.txt`、还原 `emuera.config`。

---

## 八、关键路径

- 游戏根：`d:\Game\TouHou\Yoiyami Dreamer\datas\eraTW`
- 露娜口上（真源）：`<游戏根>\ERB\口上・メッセージ関連\個人口上\006 Luna [ルナ]\露娜切露德_重制\`
- skill 工作副本：`<游戏根>\learning\eratw-skill\`（`SKILL.md` + `references/01-16` + `reference-kojo/` + `tools/`）
- 露娜同步镜像：`<游戏根>\learning\eratw-skill\reference-kojo\luna-K6\`（**改了真源就要同步这里**）
- 记忆文件：`C:\Users\lzh\.claude\projects\d--Game-TouHou-Yoiyami-Dreamer-datas-eraTW\memory\`（`MEMORY.md` 索引 + `luna-kojo-pending-todo.md` 权威现状）
- 存档备份（改名前）：`<游戏根>\sav_backup_20260717_rename\`
